{
  "name": "logparse-cli-project",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "cli": "tsx src/lib/parser-cli.ts",
    "test": "vitest run",
    "test:ci": "vitest run --pool=forks --maxWorkers=1 --no-file-parallelism",
    "test:watch": "vitest",
    "fixtures:generate": "tsx scripts/generate-encounter-fixtures.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
